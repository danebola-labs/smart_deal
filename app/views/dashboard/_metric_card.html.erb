<!-- Reusable metric card component -->
<div class="bg-white p-4 rounded-xl shadow">
    <div class="text-xs text-gray-500"><%= title %></div>
    <div class="text-2xl font-extrabold <%= color %> mt-1">
      <%= value %>
    </div>
  </div>
  