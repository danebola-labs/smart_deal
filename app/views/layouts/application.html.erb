<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Smart Deal" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Smart Deal">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <!-- Navigation -->
    <nav class="main-nav">
      <div class="nav-container">
        <div class="nav-brand">
          <%= link_to "Smart Deal", root_path, class: "nav-logo" %>
        </div>
        <div class="nav-links">
          <% if user_signed_in? %>
            <div class="nav-user-group">
              <span class="nav-user"><%= current_user.email %></span>
              <%= link_to "Cerrar Sesión", destroy_user_session_path, 
                  data: { turbo_method: :delete }, 
                  class: "nav-btn nav-btn-logout" %>
            </div>
          <% else %>
            <%= link_to "Iniciar Sesión", new_user_session_path, 
                class: "nav-btn nav-btn-login" %>
            <%= link_to "Registrarse", new_user_registration_path, 
                class: "nav-btn nav-btn-signup" %>
          <% end %>
        </div>
      </div>
    </nav>

    <!-- Flash messages -->
    <% if notice %>
      <p class="notice"><%= notice %></p>
    <% end %>
    <% if alert %>
      <p class="alert"><%= alert %></p>
    <% end %>

    <%= yield %>
  </body>
</html>
